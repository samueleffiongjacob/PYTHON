"""
    @Swapping Values in Lists
    when do u ne to swap:
    when you are shuffling things
"""

name = ["SAMUEL", "MICHELLE"]
name[0], name[1] = name[1], name[0]  # swapping the strings
print(name)
